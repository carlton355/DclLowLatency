import { LoadedModules } from '../client';
export declare class DevToolsAdapter {
    devTools: LoadedModules['DevTools'];
    exceptions: Error[];
    constructor(devTools: LoadedModules['DevTools']);
    log(...args: any[]): void;
    error(e: Error): void;
    private catchHandler;
}
