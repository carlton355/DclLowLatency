export declare type FetchFunction = typeof fetch;
export interface FetchOptions {
    canUseFetch: boolean;
    originalFetch: FetchFunction;
    previewMode: boolean;
    log(...a: any[]): void;
}
declare type Opts = {
    timeout: number;
};
export declare function createFetch({ canUseFetch, previewMode, log, originalFetch }: FetchOptions): (resource: RequestInfo, init?: (RequestInit & Partial<Opts>) | undefined) => Promise<Response>;
export {};
