export declare const DISCRETIZE_POSITION_INTERVALS: number[];
export declare type Position3D = [number, number, number];
/**
 * Calculates the discretized distance between position a and position b, using the provided intervals (DISCRETIZE_POSITION_INTERVALS as default)
 *
 * For instance, given the intervals [32, 64, 80], then we get the following values:
 * - distance(a, b) = 30 => 0
 * - distance(a, b) = 50 => 1
 * - distance(a, b) = 64 => 1
 * - distance(a, b) = 77 => 2
 * - distance(a, b) = 90 => 3
 * - distance(a, b) = 99999 => 3
 *
 * The @param intervals provided should be ordered from lower to greater
 */
export declare function discretizedPositionDistanceXZ(intervals?: number[]): (a: Position3D, b: Position3D) => number;
export declare type PeerConnectionHint = {
    id: string;
    distance: number;
    position: Position3D;
};
