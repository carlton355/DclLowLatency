import { MatrixClient } from 'matrix-js-sdk/lib/client';
import { SocialId, CurrentUserStatus, UpdateUserStatus, ProfileInfo } from './types';
import { SessionManagementAPI } from './SessionManagementAPI';
import { SocialClient } from './SocialClient';
export declare class SessionManagementClient implements SessionManagementAPI {
    private readonly matrixClient;
    private readonly socialClient;
    private loggedIn;
    constructor(matrixClient: MatrixClient, socialClient: SocialClient);
    isLoggedIn(): boolean;
    logout(): Promise<void>;
    getUserId(): SocialId;
    getDomain(): string;
    setProfileInfo({ displayName, avatarUrl }: ProfileInfo): Promise<void>;
    setStatus(status: UpdateUserStatus): Promise<void>;
    getUserStatuses(...users: SocialId[]): Map<SocialId, CurrentUserStatus>;
    onStatusChange(listener: (userId: SocialId, status: CurrentUserStatus) => void): void;
    private static eventToStatus;
    private static userToStatus;
}
